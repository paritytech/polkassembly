import { KeyringStruct } from '../types';
import { KeyringOptions, KeyringOptionInstance, KeyringSectionOption } from './types';
import { BehaviorSubject } from 'rxjs';
declare class KeyringOption implements KeyringOptionInstance {
    readonly optionsSubject: BehaviorSubject<KeyringOptions>;
    createOptionHeader(name: string): KeyringSectionOption;
    init(keyring: KeyringStruct): void;
    private linkItems;
    private addAccounts;
    private addAddresses;
    private addContracts;
    private emptyOptions;
}
declare const keyringOptionInstance: KeyringOption;
export default keyringOptionInstance;
