- args:
    cascade: false
    read_only: false
    sql: CREATE TABLE "public"."post_votes"("id" serial NOT NULL, "post_id" integer
      NOT NULL, "user_id" integer NOT NULL, "vote" bpchar NOT NULL, "created_at" timestamp
      NOT NULL DEFAULT now(), "updated_at" timestamp NOT NULL DEFAULT now(), PRIMARY
      KEY ("id") , FOREIGN KEY ("post_id") REFERENCES "public"."posts"("id") ON UPDATE
      restrict ON DELETE restrict, UNIQUE ("id"));
  type: run_sql
- args:
    name: post_votes
    schema: public
  type: add_existing_table_or_view
