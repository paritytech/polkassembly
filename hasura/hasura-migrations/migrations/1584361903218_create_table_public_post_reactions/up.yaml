- args:
    sql: CREATE TABLE "public"."post_reactions"("id" serial NOT NULL, "reaction" char
      NOT NULL, "post_id" integer NOT NULL, "user_id" integer NOT NULL, PRIMARY KEY
      ("id") , FOREIGN KEY ("post_id") REFERENCES "public"."posts"("id") ON UPDATE
      restrict ON DELETE restrict, UNIQUE ("id"));
  type: run_sql
- args:
    name: post_reactions
    schema: public
  type: add_existing_table_or_view
