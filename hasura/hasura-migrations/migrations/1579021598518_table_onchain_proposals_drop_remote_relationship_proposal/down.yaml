- args:
    hasura_fields:
    - chain_db_id
    name: proposal
    remote_field:
      proposals:
        arguments:
          where:
            proposalId: $chain_db_id
    remote_schema: chain-db
    table:
      name: onchain_proposals
      schema: public
  type: create_remote_relationship
