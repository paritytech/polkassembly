- args:
    hasura_fields:
    - onchain_motion_id
    name: onchain_motion
    remote_field:
      motion:
        arguments:
          where:
            id: $onchain_motion_id
    remote_schema: chain-db
    table:
      name: onchain_links
      schema: public
  type: create_remote_relationship
