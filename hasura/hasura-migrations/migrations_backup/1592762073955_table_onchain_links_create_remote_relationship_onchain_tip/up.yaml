- args:
    hasura_fields:
    - onchain_tip_id
    name: onchain_tip
    remote_field:
      tips:
        arguments:
          where:
            id: $onchain_tip_id
    remote_schema: chain-db
    table:
      name: onchain_links
      schema: public
  type: create_remote_relationship
