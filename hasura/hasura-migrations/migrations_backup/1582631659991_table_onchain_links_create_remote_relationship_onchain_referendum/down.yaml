- args:
    hasura_fields:
    - onchain_motion_id
    remote_field:
      motion:
        arguments:
          where:
            id: $onchain_motion_id
    remote_schema: chain-db
    table:
      name: onchain_links
      schema: public
  type: update_remote_relationship
