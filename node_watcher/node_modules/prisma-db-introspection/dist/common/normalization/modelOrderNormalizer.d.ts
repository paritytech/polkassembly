import { IGQLType, IGQLField, ISDL } from 'prisma-datamodel';
import { Normalizer } from './normalizer';
export default class ModelOrderNormalizer extends Normalizer {
    normalize(model: ISDL): void;
    normalizeType(type: IGQLType, model: ISDL): void;
    protected typeOrderComparer(ref: IGQLType[], a: IGQLType, b: IGQLType): number;
    protected fieldOrderComparer(ref: IGQLField[], aTuple: [IGQLField, number], bTuple: [IGQLField, number]): number;
    protected normalizeField(field: IGQLField, parentType: IGQLType, parentModel: ISDL): void;
}
