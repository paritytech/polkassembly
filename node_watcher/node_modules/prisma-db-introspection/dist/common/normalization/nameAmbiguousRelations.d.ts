import { ISDL, IGQLField, IGQLType } from 'prisma-datamodel';
import { Normalizer } from './normalizer';
export declare class NameAmbiguousBackRelation extends Normalizer {
    protected normalizeField(field: IGQLField, parentType: IGQLType, parentModel: ISDL): void;
}
