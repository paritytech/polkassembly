export default function testSchema(schemaSql: string, schemaName?: string, createSchema?: boolean): Promise<void>;
export declare function createAndIntrospect(schemaSql: string, schemaName?: string, createSchema?: boolean): Promise<{
    v1: string;
    v2: string;
}>;
export declare function checkSnapshot(res: {
    v1: string;
    v2: string;
}): void;
