import { GraphQLObjectType } from 'graphql/type';
import { IGQLType } from 'prisma-datamodel';
import { RootGenerator } from '../../generator';
export default class SubscriptionGenerator extends RootGenerator {
    getTypeName(input: IGQLType[], args: {}): string;
    protected shouldGenerateSubscription(type: IGQLType): boolean;
    protected generateInternal(input: IGQLType[], args: {}): GraphQLObjectType<any, any, {
        [key: string]: any;
    }>;
    private generateSubscriptionField;
}
