import { ModulesWithCalls, Registry, RegistryMetadata } from '@polkadot/types/types';
/** @internal */
export default function fromMetadata(registry: Registry, metadata: RegistryMetadata): ModulesWithCalls;
