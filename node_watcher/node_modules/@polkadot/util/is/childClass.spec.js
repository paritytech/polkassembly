"use strict";

var _interopRequireDefault = require("@babel/runtime/helpers/interopRequireDefault");

var _bn = _interopRequireDefault(require("bn.js"));

var _ = require(".");

// Copyright 2017-2020 @polkadot/util authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
describe('isChildClass', () => {
  it('returns true when a Child value', () => {
    expect((0, _.isChildClass)(_bn.default, class Test extends _bn.default {})).toEqual(true);
  });
  it('returns false on non-Child values', () => {
    expect((0, _.isChildClass)(_bn.default, Uint8Array)).toEqual(false);
  });
  it('returns false on undefined', () => {
    expect((0, _.isChildClass)(_bn.default)).toEqual(false);
  });
  it('does TS magic, determining type', () => {
    const T = class extends _bn.default {};

    if ((0, _.isChildClass)(_bn.default, T)) {
      expect(new T(12345).toNumber()).toEqual(12345);
    } else {
      throw new Error('Not a child class');
    }
  });
});