"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.ownSlashes = ownSlashes;

var _rxjs = require("rxjs");

var _operators = require("rxjs/operators");

var _util = require("../util");

// Copyright 2017-2020 @polkadot/api-derive authors & contributors
// This software may be modified and distributed under the terms
// of the Apache-2.0 license. See the LICENSE file for details.
function ownSlashes(api) {
  return (0, _util.memo)((accountId, withActive = false) => {
    return api.derive.staking.erasHistoric(withActive).pipe((0, _operators.switchMap)(eras => eras.length ? (0, _rxjs.combineLatest)(eras.map(era => (0, _rxjs.combineLatest)([(0, _rxjs.of)(era), api.query.staking.nominatorSlashInEra(era, accountId), api.query.staking.validatorSlashInEra(era, accountId)]))) : (0, _rxjs.of)([])), (0, _operators.map)(result => result.map(([era, optNom, optVal]) => ({
      era,
      total: optVal.isSome ? optVal.unwrap()[1] : optNom.unwrapOrDefault()
    }))));
  });
}